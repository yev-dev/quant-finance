# Minimal top-level project requirements (high-level, unpinned).
# Keep this list focused on core libraries used throughout the project.
# Regenerate `requirements.txt` with `pip-compile --output-file=requirements.txt requirements.in`.

streamlit
pandas
numpy
scipy
scikit-learn
statsmodels
matplotlib
seaborn
yfinance
